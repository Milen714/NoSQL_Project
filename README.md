Individual assignments 

-Hunter Pandt (726523)
* Filtering: A preloaded list of (incident) tickets can be filtered using (common) keywords. This 
is different from the search functonality because you will filter an already loaded list.  
* Can be located in the Incident Repository; GetAllIncidentsPerStatus, GetAllIncidentsByType& GetIncidentsByStatusAndType. Each should follow MongoDb
aggregation pipeline
