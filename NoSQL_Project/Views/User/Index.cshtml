@model List<User>

<h1>Users</h1>


@if (TempData["Success"] != null){
    <p class="alert-success">@TempData["Success"]</p>
}
<a class="btn btn-primary" href="/User/AddNewUser">Create New User</a>
<table>
    <thead>
        <tr><th>Name</th><th>Email</th></tr>
    </thead>
    <tbody>
        @foreach (var u in Model)
        {
            <tr>
                <td>@u.FirstName</td>
                <td>@u.Email</td>
                <td>@u.Role</td>

            </tr>
        }
    </tbody>
</table>
